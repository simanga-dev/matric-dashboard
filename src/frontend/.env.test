# Test environment defaults for CI and local test runs.
# Loaded by CI via: cp .env.test .env
# Uses Cloudflare's always-pass test keys.

API_URL=http://localhost:8080
TURNSTILE_SITE_KEY=1x00000000000000000000AA
